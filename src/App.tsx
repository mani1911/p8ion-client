import React, { useEffect } from 'react';
import Router from './Router';
import Draw from './Components/Drawer/Drawer';

const App: React.FC = () => {
	return (
		<>
			<Router />
		</>
	);
};

export default App;
